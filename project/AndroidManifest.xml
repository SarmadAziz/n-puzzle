<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="nl.mprog.N_puzzle6182097"
    android:installLocation="preferExternal"
    android:versionCode="1"
    android:versionName="1.0" >
	
    <uses-sdk
        android:minSdkVersion="7"
        android:targetSdkVersion="21" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainMenu"
            android:launchMode="singleTask"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity 
            android:name=".OpeningMenu"
            android:launchMode="singleTask">
            <!-- singletask om naar dezelfde activity, ipv nieuw maken -->
        </activity>
        <activity android:name=".PicList"> </activity>
        <activity android:name=".Game"
            	  android:screenOrientation="portrait"> 
        </activity>
        <activity android:name=".Won"> </activity>
        <activity android:name=".ScoreBoard"></activity>
        
    </application>
	
</manifest>
